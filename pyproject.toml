[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "remix-re"
version = "3.0.0"
description = "Correlated Android/ARM64 reverse-engineering engine"
requires-python = ">=3.11"
authors = [{name="REMIX CTF Lab"}]
license = {text="MIT"}
dependencies = []

[project.scripts]
remix = "remix.v3cli:main"

[tool.setuptools.packages.find]
include = ["remix*"]
